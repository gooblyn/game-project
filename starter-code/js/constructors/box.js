// Constructor for the objects "box".
// Each box of the board could be just of one type: player1 (1), player2 (2) or empty (0).
// By default, every box will be empty.
// It has, also, two properties for saving its coordenates(x,y)

function Box() {
  this.player = 0; //empty
  this.x = 0;
  this.y = 0;
};
